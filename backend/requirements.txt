fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
python-dotenv==1.0.1
openai==1.51.0
# PR R — multipart parsing for POST /api/runway/upload-image. FastAPI
# `UploadFile` / `File` depends on this; otherwise import-time crash.
python-multipart==0.0.20
